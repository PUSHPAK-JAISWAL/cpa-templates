# MLOps GitHub Actions (extension bank)

CI / Continuous Training / model gate / deploy workflow stubs for MLOps templates
([#87](https://github.com/Create-Python-App/cpa-templates/issues/87)).

Compatible with `mlops-sklearn` (and future pytorch/tensorflow types). CPU-first;
`workflow_dispatch` enabled. No GPU runners by default.
