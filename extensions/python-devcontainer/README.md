# Dev Container

Open this folder in a [Dev Container](https://code.visualstudio.com/docs/devcontainers/containers) for a reproducible Python + uv environment.

```sh
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
