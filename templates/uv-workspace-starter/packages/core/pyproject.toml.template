[project]
name = "{{ packagePrefix }}-core"
version = "0.1.0"
description = "Shared core library for the {{ packagePrefix }} workspace"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/core"]
