# Environment

Member apps may ship their own `.env.example`. Keep secrets out of the repo;
this root file is a pointer for workspace-wide conventions.

# Example (override per app as needed)
# CLI_LOG_LEVEL=INFO
